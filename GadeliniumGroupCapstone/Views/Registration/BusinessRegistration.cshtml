@model GadeliniumGroupCapstone.Models.Business

@{
    ViewData["Title"] = "BusinessRegistration";
}

<h1>BusinessRegistration</h1>

<h4>Business</h4>
<hr />
<div class="row">
    <div class="col-md-8">
        <form asp-action="CreateBusinessAccountWithProfileImage" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Business Name</label>
                <input asp-for="BusinessName" class="form-control w-50" />
                <span asp-validation-for="BusinessName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Address" class="control-label"></label>
                <input asp-for="Address" class="form-control  w-50" />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Zip" class="control-label"></label>
                <input asp-for="Zip" class="form-control  w-50" />
                <span asp-validation-for="Zip" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="row">
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.MondayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Monday Opening Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.TuesdayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Tuesday Opening Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.WednesdayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Wednesday Opening Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.ThursdayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Thursday Opening Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.FridayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Friday Opening Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.SaturdayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Saturday Opening Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.SundayOpening">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Sunday Opening Time</label>
                    </div>


                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.MondayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Monday Closing Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.TuesdayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Tuesday Closing Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.WednesdayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Tuesday Closing Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.ThursdayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Thursday Closing Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.FridayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Friday Closing Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.SaturdayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Saturday Closing Time</label>
                    </div>
                    <div class="col-6">
                        <select class="mr-3" asp-for="BusinessHour.SundayClosing">
                            @for (int i = 1; i < 13; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                        <label>Sunday Closing Time</label>
                    </div>
                </div>

            </div>
            <div class="form-group">
                <label asp-for="Phone" class="control-label"></label>
                <input asp-for="Phone" class="form-control  w-50" />
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <input type="file" asp-for="UploadFile" name="uploadFile" />
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Home" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
